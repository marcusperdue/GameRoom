{
  "name": "gameroom",
  "version": "0.1.0",
  "private": true,
  "description": "Portable console launcher for GameCube, Wii, PS2, Xbox, Nintendo DS, PS1, and PSP.",
  "author": "Marcus Perdue",
  "homepage": "https://marcusperdue.github.io/GameRoom/",
  "repository": {
    "type": "git",
    "url": "https://github.com/marcusperdue/GameRoom.git"
  },
  "desktopName": "GameRoom",
  "main": "src/main.js",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node --check src/modules/*.js && node --check tests/*.test.js",
    "test": "node --test tests/*.test.js",
    "scan": "node ../Scripts/scan-library.mjs",
    "init": "node ../Scripts/init-folders.mjs",
    "pack": "electron-builder --dir",
    "dist:mac": "electron-builder --mac dmg zip --arm64",
    "dist:mac:split": "electron-builder --mac dmg zip --arm64 --x64",
    "dist:linux": "electron-builder --linux AppImage tar.gz --arm64 --x64",
    "dist:win": "electron-builder --win nsis zip --arm64 --x64",
    "dist:all": "npm run dist:mac && npm run dist:linux && npm run dist:win"
  },
  "devDependencies": {
    "@electron/notarize": "^2.5.0",
    "electron": "^33.3.1",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "local.gameroom.launcher",
    "productName": "GameRoom",
    "icon": "src/assets/icon.png",
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "afterSign": "scripts/notarize-mac.js",
    "directories": {
      "output": "../Downloads/Updates/builds"
    },
    "files": [
      "src/**/*",
      "package.json"
    ],
    "mac": {
      "icon": "src/assets/icon.icns",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.games"
    },
    "linux": {
      "icon": "src/assets/icon.png",
      "syncDesktopName": true,
      "target": [
        "AppImage",
        "tar.gz"
      ],
      "category": "Game"
    },
    "win": {
      "icon": "src/assets/icon.ico",
      "target": [
        "nsis",
        "zip"
      ]
    }
  }
}
